[package]
name = "raxis-proc-macro"
version = "0.1.0"
edition = "2024"

[lib]
proc-macro = true

[dependencies]
quote = "1.0.40"
syn = { version = "2.0.106", features = ["full", "extra-traits", "parsing"] }
proc-macro2 = "1.0"
raxis-core = { path = "../raxis-core" }
